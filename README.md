# textbase-fs

Textbase fuse filesystem

Mount https://textbase.scriptorium.ro as a local filesystem.

Works on node 18.

Build: 
```bash
$ npm install 
$ npm run build
```

Run:
```bash
$ npm run index
```